services:
  db:
    image: postgres:16-alpine
    container_name: vittion-db
    restart: always
    environment:
      - POSTGRES_USER=vittion
      - POSTGRES_PASSWORD=vittion
      - POSTGRES_DB=vittion
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vittion -d vittion"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
      network: host
    container_name: vittion-backend
    restart: always
    environment:
      - DATABASE_URL=postgresql://vittion:vittion@db:5432/vittion
      - FLASK_ENV=development
      - FLASK_APP=app.py
    ports:
      - "5000:5000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backend:/app
      - ./src/lib/data:/app/src/lib/data

volumes:
  postgres_data:
